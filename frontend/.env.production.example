# Copy to .env.production (or set as CI env vars) before `npm run cf:deploy`.
# VITE_API_BASE_URL is baked into the static bundle at build time, since this
# is a static SPA with no server-side runtime to read it from at request time.
VITE_API_BASE_URL=https://api.want-to-go.example.com
