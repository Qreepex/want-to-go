# Template only - do not commit real values or `kubectl apply` this file directly.
# Create the actual secret out-of-band instead, e.g.:
#   kubectl create secret generic backend-secrets -n want-to-go \
#     --from-literal=DATABASE_URL=... \
#     --from-literal=REDIS_URL=... \
#     --from-literal=JWT_SECRET=... \
#     --from-literal=AUTHENTIK_CLIENT_ID=... \
#     --from-literal=AUTHENTIK_CLIENT_SECRET=... \
#     --from-literal=AUTHENTIK_ISSUER_URL=... \
#     --from-literal=AUTHENTIK_REDIRECT_URI=... \
#     --from-literal=AWS_ACCESS_KEY_ID=... \
#     --from-literal=AWS_SECRET_ACCESS_KEY=...
# or manage it with sealed-secrets / external-secrets in a real GitOps setup.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: want-to-go
type: Opaque
stringData:
  DATABASE_URL: "postgres://CHANGEME:CHANGEME@CHANGEME:5432/want-to-go"
  REDIS_URL: "redis://CHANGEME:6379"
  JWT_SECRET: "CHANGEME"
  AUTHENTIK_CLIENT_ID: "CHANGEME"
  AUTHENTIK_CLIENT_SECRET: "CHANGEME"
  AUTHENTIK_ISSUER_URL: "https://CHANGEME/application/o/want-to-go"
  AUTHENTIK_REDIRECT_URI: "https://want-to-go.example.com/auth/callback"
  AWS_ACCESS_KEY_ID: "CHANGEME"
  AWS_SECRET_ACCESS_KEY: "CHANGEME"
